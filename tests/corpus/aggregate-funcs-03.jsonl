{"id":"aggfc0001","category":"aggregate-funcs","sql":"SELECT count(*), count(val), count(DISTINCT val), sum(val), avg(val), min(val), max(val) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"count(*)/count(expr)/count(DISTINCT)/sum/avg/min/max basic aggregates"}
{"id":"aggfc0002","category":"aggregate-funcs","sql":"SELECT array_agg(name ORDER BY name), string_agg(name, ', ' ORDER BY name DESC), json_agg(data), jsonb_agg(data), jsonb_object_agg(name, val) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"array_agg with ORDER BY, string_agg with delimiter and ORDER BY, json_agg, jsonb_agg, jsonb_object_agg"}
{"id":"aggfc0003","category":"aggregate-funcs","sql":"SELECT bool_and(flag), bool_or(flag), every(flag), bit_and(qty), bit_or(qty), bit_xor(qty) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"bool_and/bool_or/every and bit_and/bit_or/bit_xor aggregates"}
{"id":"aggfc0004","category":"aggregate-funcs","sql":"SELECT count(*) FILTER (WHERE val > 0), sum(val) FILTER (WHERE flag), avg(qty) FILTER (WHERE qty > 0) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"FILTER (WHERE ..) clause on standard aggregates"}
{"id":"aggfc0005","category":"aggregate-funcs","sql":"SELECT percentile_cont(0.5) WITHIN GROUP (ORDER BY val), percentile_disc(0.25) WITHIN GROUP (ORDER BY qty DESC), mode() WITHIN GROUP (ORDER BY name) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"ordered-set aggregates: percentile_cont, percentile_disc, mode with WITHIN GROUP (ORDER BY)"}
{"id":"aggfc0006","category":"aggregate-funcs","sql":"SELECT status, rank(2) WITHIN GROUP (ORDER BY qty), dense_rank(2) WITHIN GROUP (ORDER BY qty), percent_rank(2) WITHIN GROUP (ORDER BY qty), cume_dist(2) WITHIN GROUP (ORDER BY qty) FROM s.t GROUP BY status","expect":"ok","ref":"functions-aggregate","note":"hypothetical-set aggregates: rank/dense_rank/percent_rank/cume_dist WITHIN GROUP"}
{"id":"aggfc0007","category":"aggregate-funcs","sql":"SELECT corr(qty, val), covar_pop(qty, val), covar_samp(qty, val), stddev(val), stddev_pop(val), stddev_samp(val), variance(val), var_pop(val), var_samp(val) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"statistical aggregates: corr/covar_pop/covar_samp/stddev/variance variants"}
{"id":"aggfc0008","category":"aggregate-funcs","sql":"SELECT status, grouping(status), count(*) FROM s.t GROUP BY ROLLUP(status)","expect":"ok","ref":"functions-aggregate","note":"grouping() function with ROLLUP grouping set"}
{"id":"aggfc0009","category":"aggregate-funcs","sql":"SELECT range_agg(span), range_intersect_agg(span) FROM s.t","expect":"ok","ref":"functions-aggregate","note":"range_agg and range_intersect_agg on int4range column"}
{"id":"aggfc0010","category":"aggregate-funcs","sql":"SELECT sum(val) FILTER (val > 0) FROM s.t","expect":"error","ref":"functions-aggregate","note":"syntax error: FILTER clause missing WHERE keyword"}
{"id":"aggfc0011","category":"aggregate-funcs","sql":"SELECT percentile_cont(0.5) FROM s.t","expect":"error","ref":"functions-aggregate","note":"syntax error: ordered-set agg percentile_cont missing WITHIN GROUP clause"}
{"id":"aggfc0012","category":"aggregate-funcs","sql":"SELECT count(*) WITHIN GROUP (ORDER BY val) FROM s.t","expect":"error","ref":"functions-aggregate","note":"semantic error: WITHIN GROUP on a regular (non-ordered-set) aggregate"}
