// <auto-generated> Generated from tests/corpus/*.jsonl by CorpusTestGenerator. Do not edit by hand. </auto-generated>
using Xunit;

namespace PgProj.Core.Tests.Corpus;

public class Corpus_PlpgsqlTriggerBody
{
    [Fact]
    public void pptba0001() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_basic() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0002() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_old() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0003() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_null() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0004() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0005() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op_update() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'UPDATE' THEN RETURN NEW; END IF; RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0006() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op_delete() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'DELETE' THEN RETURN OLD; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0007() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op_truncate() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'TRUNCATE' THEN RETURN NULL; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0008() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_all_ops() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'DELETE' THEN RETURN OLD; ELSIF TG_OP = 'UPDATE' THEN RETURN NEW; ELSIF TG_OP = 'INSERT' THEN RETURN NEW; ELSE RETURN NULL; END IF; END $$;", "ok");
    [Fact]
    public void pptba0009() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_name() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE n name; BEGIN n := TG_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0010() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_when() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE w text; BEGIN w := TG_WHEN; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0011() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_level() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE lv text; BEGIN lv := TG_LEVEL; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0012() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_relid() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE r oid; BEGIN r := TG_RELID; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0013() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_table_name() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE tn name; BEGIN tn := TG_TABLE_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0014() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_table_schema() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE ts name; BEGIN ts := TG_TABLE_SCHEMA; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0015() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_nargs() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE n integer; BEGIN n := TG_NARGS; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0016() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_argv() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE a text; BEGIN a := TG_ARGV[0]; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0017() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_argv1() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE a text; BEGIN a := TG_ARGV[1]; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0018() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_nargs_check() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_NARGS > 0 THEN RAISE NOTICE 'arg0=%', TG_ARGV[0]; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0019() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_new_field_read() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v text; BEGIN v := NEW.name; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0020() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_old_field_read() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v text; BEGIN v := OLD.name; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0021() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_modify_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := 'modified'; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0022() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_modify_new_qty() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.qty := NEW.qty + 1; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0023() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_modify_new_flag() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.flag := true; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0024() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_new_null_guard() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.name IS NULL THEN RAISE EXCEPTION 'name cannot be null'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0025() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_notice() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'trigger fired: %', TG_OP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0026() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_exception() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE EXCEPTION 'operation not allowed: %', TG_OP; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0027() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_warning() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE WARNING 'unexpected op %', TG_OP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0028() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_info() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE INFO 'table: %, op: %', TG_TABLE_NAME, TG_OP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0029() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_log() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE LOG 'trigger % on %', TG_NAME, TG_TABLE_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0030() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_debug() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE DEBUG 'schema=%', TG_TABLE_SCHEMA; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0031() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_fmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE EXCEPTION 'name invalid' USING ERRCODE = 'check_violation'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0032() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_suppress_insert() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' AND NEW.qty < 0 THEN RETURN NULL; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0033() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_suppress_update() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.val < OLD.val THEN RETURN NULL; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0034() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_when_check() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_WHEN = 'BEFORE' THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0035() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_after_stmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_WHEN = 'AFTER' AND TG_LEVEL = 'STATEMENT' THEN RAISE NOTICE 'after statement trigger'; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0036() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_level_check() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_LEVEL = 'ROW' THEN RETURN NEW; ELSE RETURN NULL; END IF; END $$;", "ok");
    [Fact]
    public void pptba0037() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_relid_use() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE tn text; BEGIN SELECT relname INTO tn FROM pg_class WHERE oid = TG_RELID; RAISE NOTICE 'table=%', tn; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0038() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_compare_names() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_TABLE_NAME <> TG_NAME THEN RAISE NOTICE 'table and trigger names differ'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0039() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_multi_raise() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'op=%', TG_OP; RAISE NOTICE 'tbl=%', TG_TABLE_NAME; RAISE NOTICE 'schema=%', TG_TABLE_SCHEMA; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0040() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_insert_audit() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RAISE NOTICE 'inserted id=%', NEW.id; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0041() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_delete_audit() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'DELETE' THEN RAISE NOTICE 'deleted id=%', OLD.id; END IF; RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0042() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_update_compare() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.name <> OLD.name THEN RAISE NOTICE 'name changed from % to %', OLD.name, NEW.name; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0043() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_conditional_modify() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN NEW.flag := true; ELSIF TG_OP = 'UPDATE' THEN NEW.qty := NEW.qty + 1; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0044() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_declare_vars() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE old_name text; new_name text; BEGIN old_name := OLD.name; new_name := NEW.name; RAISE NOTICE '% -> %', old_name, new_name; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0045() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_or_replace() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;
CREATE OR REPLACE FUNCTION s.trg_or_replace() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0046() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_dollar_quote() RETURNS trigger LANGUAGE plpgsql AS $body$ BEGIN RETURN NEW; END $body$;", "ok");
    [Fact]
    public void pptba0047() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_dollar_tag2() RETURNS trigger LANGUAGE plpgsql AS $trg$ BEGIN RETURN NEW; END $trg$;", "ok");
    [Fact]
    public void pptba0048() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_argv_loop() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN 0..TG_NARGS-1 LOOP RAISE NOTICE 'arg[%]=%', i, TG_ARGV[i]; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0049() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_returns_new_explicit() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; /* explicit RETURN NEW */ END $$;", "ok");
    [Fact]
    public void pptba0050() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_perform_call() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN PERFORM s.f(1); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0051() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_no_args_decl() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0052() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_with_param(x integer) RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "error");
    [Fact]
    public void pptba0053() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_return() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN 42; END $$;", "ok");
    [Fact]
    public void pptba0054() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_no_return() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'hi'; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0055() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_no_parens RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0056() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_missing_begin() RETURNS trigger LANGUAGE plpgsql AS $$ RETURN NEW; END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0057() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_no_as() RETURNS trigger LANGUAGE plpgsql $body$ BEGIN RETURN NEW; END $body$;", "error");
    [Fact]
    public void pptba0058() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_int_var() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v integer := 5; BEGIN RETURN v; END $$;", "ok");
    [Fact]
    public void pptba0059() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_text() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v text := 'hi'; BEGIN RETURN v; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0060() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_lang() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW END $$;", "error");
    [Fact]
    public void pptba0061() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exception_handler() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.qty < 0 THEN RAISE EXCEPTION 'qty must be non-negative, got %', NEW.qty; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0062() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exception_hint() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE EXCEPTION 'invalid' USING HINT = 'provide a positive value'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0063() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exception_detail() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE EXCEPTION 'bad data' USING DETAIL = 'field name was empty'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0064() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_created_at() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.created_at := now(); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0065() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_status() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.status := 'happy'::s.mood; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0066() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_jsonb() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.data := jsonb_build_object('op', TG_OP, 'ts', now()); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0067() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_tags() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.tags := ARRAY['auto']; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0068() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_append_tag() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.tags := NEW.tags || ARRAY[TG_OP]; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0069() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_check_old_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF OLD.val IS DISTINCT FROM NEW.val THEN RAISE NOTICE 'val changed'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0070() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_select_into() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cnt integer; BEGIN SELECT count(*) INTO cnt FROM s.t WHERE name = NEW.name; IF cnt > 0 THEN RAISE EXCEPTION 'duplicate name %', NEW.name; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0071() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_found_check() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE r s.t; BEGIN SELECT * INTO r FROM s.t WHERE id = NEW.id; IF NOT FOUND THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0072() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_nested_if() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN IF NEW.qty < 0 THEN RETURN NULL; ELSE RETURN NEW; END IF; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0073() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_case_stmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN CASE TG_OP WHEN 'INSERT' THEN RETURN NEW; WHEN 'UPDATE' THEN RETURN NEW; WHEN 'DELETE' THEN RETURN OLD; ELSE RETURN NULL; END CASE; END $$;", "ok");
    [Fact]
    public void pptba0074() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exception_block() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; EXCEPTION WHEN others THEN RAISE NOTICE 'error caught'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0075() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exception_sqlstate() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN INSERT INTO s.t(name, qty, flag, status) VALUES (NEW.name, NEW.qty, NEW.flag, NEW.status); RETURN NEW; EXCEPTION WHEN unique_violation THEN RAISE NOTICE 'duplicate ignored'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0076() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_while_loop() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int := 0; BEGIN WHILE i < TG_NARGS LOOP RAISE NOTICE 'argv[%]=%', i, TG_ARGV[i]; i := i + 1; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0077() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_loop_exit() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int := 0; BEGIN LOOP EXIT WHEN i >= TG_NARGS; i := i + 1; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0078() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_for_in_query() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE r record; BEGIN FOR r IN SELECT id, name FROM s.t LIMIT 3 LOOP RAISE NOTICE 'id=%', r.id; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0079() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_null_stmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0080() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_when_after() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_WHEN = 'AFTER' THEN RETURN NULL; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0081() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_when_instead() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_WHEN = 'INSTEAD OF' THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0082() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_returns_void2() RETURNS void LANGUAGE plpgsql AS $$ BEGIN RETURN; END $$;
CREATE TRIGGER t_bad BEFORE INSERT ON s.t FOR EACH ROW EXECUTE FUNCTION s.trg_returns_void2();", "error");
    [Fact]
    public void pptba0083() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_argv_loop2() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; v text; BEGIN FOR i IN 0..TG_NARGS-1 LOOP v := TG_ARGV[i]; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0084() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_relname() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE rn name; BEGIN rn := TG_RELNAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0085() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_insert_select_star() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RAISE NOTICE 'new row: %', NEW; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0086() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_delete_return_old() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'deleting: %', OLD; RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0087() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_complex_validation() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.name IS NULL OR length(trim(NEW.name)) = 0 THEN RAISE EXCEPTION 'name cannot be empty'; END IF; IF NEW.qty < 0 THEN RAISE EXCEPTION 'qty must be >= 0, got %', NEW.qty; END IF; NEW.flag := (NEW.qty > 0); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0088() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_get_diagnostics() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE r_count bigint; BEGIN GET DIAGNOSTICS r_count = ROW_COUNT; RAISE NOTICE 'rows=%', r_count; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0089() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_simple_comment() RETURNS trigger LANGUAGE plpgsql AS $body$ BEGIN /* block comment in trigger */ RETURN NEW; END $body$;", "ok");
    [Fact]
    public void pptba0090() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_schema_qualify() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE tn text; BEGIN tn := TG_TABLE_SCHEMA || '.' || TG_TABLE_NAME; RAISE NOTICE 'full table=%', tn; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0091() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_dynamic_sql() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE sql_str text; BEGIN sql_str := 'INSERT INTO s.t2(t_id, label) VALUES ($1, $2)'; EXECUTE sql_str USING NEW.id, TG_OP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0092() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_execute_into() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cnt integer; BEGIN EXECUTE 'SELECT count(*) FROM s.t' INTO cnt; RAISE NOTICE 'count=%', cnt; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0093() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_hstore_style() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v jsonb; BEGIN v := NEW.data; IF v IS NOT NULL AND (v->>'active')::boolean THEN NEW.flag := true; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0094() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_array_elem() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.tags IS NOT NULL AND array_length(NEW.tags, 1) > 0 THEN RAISE NOTICE 'first tag=%', NEW.tags[1]; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0095() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_null_check_all() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW IS NULL THEN RAISE EXCEPTION 'NEW cannot be null at row level'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0096() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op_unknown() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP NOT IN ('INSERT','UPDATE','DELETE','TRUNCATE') THEN RAISE EXCEPTION 'unknown op: %', TG_OP; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0097() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_multiple_returns() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'DELETE' THEN RETURN OLD; END IF; IF TG_OP = 'INSERT' THEN RETURN NEW; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0098() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_coalesce() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := coalesce(NEW.name, 'unnamed'); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0099() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_to_lower() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := lower(NEW.name); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0100() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_trim_name() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := trim(NEW.name); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0101() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_upper_name() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP IN ('INSERT', 'UPDATE') THEN NEW.name := upper(NEW.name); END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0102() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_label_syntax_err() RETURNS trigger LANGUAGE plpgsql AS $$ <<outer>> BEGIN RETURN NEW; END outer $$;", "ok");
    [Fact]
    public void pptba0103() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_nested_block() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN BEGIN NEW.qty := NEW.qty + 1; END; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0104() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_nested_exception() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN BEGIN RAISE EXCEPTION 'inner'; EXCEPTION WHEN others THEN NULL; END; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0105() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_op_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE op text; BEGIN op := TG_OP; RAISE NOTICE 'op=%', op; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0106() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_name_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE n name; BEGIN n := TG_NAME; RAISE NOTICE 'name=%', n; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0107() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_level_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE lv text; BEGIN lv := TG_LEVEL; RAISE NOTICE 'level=%', lv; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0108() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_when_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE w text; BEGIN w := TG_WHEN; RAISE NOTICE 'when=%', w; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0109() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_relid_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE r oid; BEGIN r := TG_RELID; RAISE NOTICE 'relid=%', r; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0110() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_nargs_read_only() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE n integer; BEGIN n := TG_NARGS; RAISE NOTICE 'nargs=%', n; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0111() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_declare_var() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE 123bad text; BEGIN RETURN NEW; END $$;", "error");
    [Fact]
    public void pptba0112() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_if_noend() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RETURN NEW; END $$;", "error");
    [Fact]
    public void pptba0113() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_loop_noend() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN LOOP RAISE NOTICE 'x'; END $$;", "error");
    [Fact]
    public void pptba0114() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_case_noend() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN CASE TG_OP WHEN 'INSERT' THEN RETURN NEW; END $$;", "error");
    [Fact]
    public void pptba0115() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_raise_level() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE BADLEVEL 'msg'; RETURN NEW; END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0116() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_for_syntax() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN FOR IN 1..5 LOOP RETURN NEW; END LOOP; END $$;", "error");
    [Fact]
    public void pptba0117() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_current_user() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := current_user; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0118() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_session_user() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'user=%', session_user; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0119() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_current_timestamp() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.created_at := current_timestamp; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0120() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_clock_timestamp() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.created_at := clock_timestamp(); RETURN NEW; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0121() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_setof_returns() RETURNS SETOF s.t LANGUAGE plpgsql AS $$ BEGIN RETURN; END $$;
CREATE TRIGGER t_bad2 BEFORE INSERT ON s.t FOR EACH ROW EXECUTE FUNCTION s.trg_setof_returns();", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0122() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_int_returns() RETURNS integer LANGUAGE plpgsql AS $$ BEGIN RETURN 1; END $$;
CREATE TRIGGER t_bad3 BEFORE INSERT ON s.t FOR EACH ROW EXECUTE FUNCTION s.trg_int_returns();", "error");
    [Fact]
    public void pptba0123() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_argv_nargs_zero() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE a text; BEGIN IF TG_NARGS = 0 THEN a := NULL; ELSE a := TG_ARGV[0]; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0124() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_raise_no_fmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'trigger fired'; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0125() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_insert_with_returning() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE new_id bigint; BEGIN INSERT INTO s.t2(t_id, label) VALUES (NEW.id, TG_NAME) RETURNING id INTO new_id; RAISE NOTICE 'inserted t2 id=%', new_id; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0126() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_update_stmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN UPDATE s.t2 SET label = TG_OP WHERE t_id = NEW.id; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0127() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_delete_stmt() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN DELETE FROM s.t2 WHERE t_id = OLD.id; RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0128() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_long_declare() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE a text; b integer; c boolean; d timestamptz; e name; BEGIN a := TG_TABLE_NAME; b := TG_NARGS; c := (TG_OP = 'INSERT'); d := now(); e := TG_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0129() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_not_null_final() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.name IS NOT NULL AND NEW.qty >= 0 THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0130() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_tg_argv_string_compare() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_NARGS > 0 AND TG_ARGV[0] = 'audit' THEN RAISE NOTICE 'audit mode'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0131() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_all_specials_read() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE n name; w text; lv text; op text; relid oid; tname name; tschema name; nargs integer; BEGIN n := TG_NAME; w := TG_WHEN; lv := TG_LEVEL; op := TG_OP; relid := TG_RELID; tname := TG_TABLE_NAME; tschema := TG_TABLE_SCHEMA; nargs := TG_NARGS; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0132() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_concat_argv() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE result text := ''; i int; BEGIN FOR i IN 0..TG_NARGS-1 LOOP result := result || TG_ARGV[i]; IF i < TG_NARGS - 1 THEN result := result || ','; END IF; END LOOP; RAISE NOTICE 'args=%', result; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0133() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_search_path_qualify() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_TABLE_SCHEMA = 's' THEN RAISE NOTICE 'in schema s'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0134() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_new_val_cast() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.val := NEW.val::numeric(12,2); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0135() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_old_val_compare() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF OLD.val > NEW.val THEN RAISE EXCEPTION 'val cannot decrease from % to %', OLD.val, NEW.val; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0136() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_unlogged() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0137() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_security_definer() RETURNS trigger SECURITY DEFINER LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0138() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_security_invoker() RETURNS trigger SECURITY INVOKER LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0139() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_cost_option() RETURNS trigger LANGUAGE plpgsql COST 100 AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0140() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_option() RETURNS trigger LANGUAGE plpgsql SET search_path = s, public AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0141() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_volatile() RETURNS trigger VOLATILE LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0142() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_stable_ok() RETURNS trigger STABLE LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0143() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_immutable_ok() RETURNS trigger IMMUTABLE LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0144() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_row_insert_only() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP <> 'INSERT' THEN RAISE EXCEPTION 'only INSERT supported'; END IF; NEW.flag := true; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0145() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_update_only() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP <> 'UPDATE' THEN RETURN NEW; END IF; IF OLD.qty <> NEW.qty THEN RAISE NOTICE 'qty updated: % -> %', OLD.qty, NEW.qty; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0146() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_new_id_immutable() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'UPDATE' AND NEW.id <> OLD.id THEN RAISE EXCEPTION 'id is immutable'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0147() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_before_delete_archive() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN INSERT INTO s.t2(t_id, label) VALUES (OLD.id, 'archived'); RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptba0148() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_row_count() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cnt bigint; BEGIN SELECT count(*) INTO cnt FROM s.t; RAISE NOTICE 'rows in t: %', cnt; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0149() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_reuse_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.qty := NEW.qty * 2; NEW.val := NEW.val + 1.00; NEW.flag := NOT NEW.flag; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0150() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_status_enum_check() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.status NOT IN ('sad'::s.mood, 'ok'::s.mood, 'happy'::s.mood) THEN RAISE EXCEPTION 'invalid status'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0151() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_range_check() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.span IS NOT NULL AND NOT isempty(NEW.span) THEN RAISE NOTICE 'span=%', NEW.span; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0152() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_composite_addr() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF (NEW.home).city IS NULL THEN NEW.home := ROW('unknown', 'unknown', '00000')::s.addr; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0153() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_set_default_val() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.val IS NULL THEN NEW.val := 0.00; END IF; RETURN NEW; END $$;", "ok");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptba0154() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_return_keyword() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEXT; END $$;", "error");
    [Fact]
    public void pptba0155() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_elsif_syntax() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RETURN NEW; ELSEIF TG_OP = 'UPDATE' THEN RETURN NEW; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptba0156() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_assign_op() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.qty = NEW.qty + 1; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0157() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_for_in_integer() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN 1..5 LOOP RAISE NOTICE 'i=%', i; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0158() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_for_reverse() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN REVERSE 5..1 LOOP RAISE NOTICE 'i=%', i; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0159() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_for_by() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN 0..10 BY 2 LOOP RAISE NOTICE 'i=%', i; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0160() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_continue() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN 1..5 LOOP CONTINUE WHEN i = 3; RAISE NOTICE 'i=%', i; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0161() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_exit_labeled() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN <<outer_loop>> FOR i IN 1..10 LOOP EXIT outer_loop WHEN i > 3; END LOOP outer_loop; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0162() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_assert() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN ASSERT TG_OP IS NOT NULL, 'TG_OP must not be null'; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0163() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_assert_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN ASSERT NEW IS NOT NULL OR TG_OP = 'DELETE', 'NEW should only be null on DELETE'; RETURN CASE WHEN TG_OP = 'DELETE' THEN OLD ELSE NEW END; END $$;", "ok");
    [Fact]
    public void pptba0164() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_get_diag_pg_context() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE ctx text; BEGIN GET DIAGNOSTICS ctx = PG_CONTEXT; RAISE NOTICE 'ctx=%', ctx; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0165() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_get_diag_pg_exception() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE msg text; BEGIN BEGIN NULL; EXCEPTION WHEN others THEN GET STACKED DIAGNOSTICS msg = MESSAGE_TEXT; RAISE NOTICE 'err=%', msg; END; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0166() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_open_cursor() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cur refcursor; r record; BEGIN OPEN cur FOR SELECT id, name FROM s.t LIMIT 1; FETCH cur INTO r; CLOSE cur; RAISE NOTICE 'id=%', r.id; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0167() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_cursor_found() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cur refcursor; r record; BEGIN OPEN cur FOR SELECT id FROM s.t WHERE id = NEW.id; FETCH cur INTO r; IF FOUND THEN RAISE NOTICE 'row exists'; END IF; CLOSE cur; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0168() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_open_cursor_insert() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE cur refcursor; BEGIN OPEN cur FOR INSERT INTO s.t(name, qty, flag, status) VALUES('x', 1, false, 'ok'); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptba0169() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_return_new_row_level() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;
CREATE TRIGGER t_row_trg BEFORE INSERT ON s.t FOR EACH ROW EXECUTE FUNCTION s.trg_return_new_row_level();", "ok");
    [Fact]
    public void pptba0170() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_stmt_level() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'statement trigger: op=%, tbl=%', TG_OP, TG_TABLE_NAME; RETURN NULL; END $$;
CREATE TRIGGER t_stmt_trg AFTER INSERT ON s.t FOR EACH STATEMENT EXECUTE FUNCTION s.trg_stmt_level();", "ok");
    [Fact]
    public void pptbb0001() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_ret_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0002() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_ret_old() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptbb0003() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_ret_null() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptbb0004() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_op() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' THEN RETURN NEW; ELSIF TG_OP = 'UPDATE' THEN RETURN NEW; ELSIF TG_OP = 'DELETE' THEN RETURN OLD; END IF; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptbb0005() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_name() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_name text; BEGIN v_name := TG_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0006() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_when() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_when text; BEGIN v_when := TG_WHEN; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0007() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_level() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_level text; BEGIN v_level := TG_LEVEL; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0008() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_relid() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_oid oid; BEGIN v_oid := TG_RELID; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0009() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_table_name() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_name text; BEGIN v_name := TG_TABLE_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0010() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_table_schema() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_schema text; BEGIN v_schema := TG_TABLE_SCHEMA; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0011() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_nargs() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_n int; BEGIN v_n := TG_NARGS; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0012() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_tg_argv() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE v_arg text; BEGIN IF TG_NARGS > 0 THEN v_arg := TG_ARGV[0]; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0013() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_modify_new() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := upper(NEW.name); RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0014() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_modify_new_multi() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := coalesce(NEW.name, 'default'); NEW.qty := coalesce(NEW.qty, 0); NEW.flag := true; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0015() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_raise_notice() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'trigger fired: % on %', TG_OP, TG_TABLE_NAME; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0016() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_raise_exception() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.val < 0 THEN RAISE EXCEPTION 'val cannot be negative, got %', NEW.val; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0017() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_stmt_level() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'statement trigger fired'; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptbb0018() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_conditional_suppress() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'DELETE' AND OLD.flag = true THEN RETURN NULL; END IF; RETURN OLD; END $$;", "ok");
    [Fact]
    public void pptbb0019() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_all_tg_vars() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE info text; BEGIN info := format('op=%s when=%s level=%s table=%s.%s nargs=%s', TG_OP, TG_WHEN, TG_LEVEL, TG_TABLE_SCHEMA, TG_TABLE_NAME, TG_NARGS); RAISE NOTICE '%', info; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0020() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_multiple_argv() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE i int; BEGIN FOR i IN 0 .. TG_NARGS - 1 LOOP RAISE NOTICE 'arg[%] = %', i, TG_ARGV[i]; END LOOP; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0021() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_insert_only() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP <> 'INSERT' THEN RAISE EXCEPTION 'this trigger only handles INSERT'; END IF; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0022() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_truncate() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RAISE NOTICE 'TRUNCATE on %.%', TG_TABLE_SCHEMA, TG_TABLE_NAME; RETURN NULL; END $$;", "ok");
    [Fact]
    public void pptbb0023() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_relid_cast() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE tbl text; BEGIN tbl := TG_RELID::regclass::text; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0024() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_exception_handler() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN NEW.name := upper(NEW.name); RETURN NEW; EXCEPTION WHEN others THEN RAISE WARNING 'trigger error: %', SQLERRM; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0025() => CorpusAssert.Parses(@"CREATE OR REPLACE FUNCTION s.trg_argv_text() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE col_name text; BEGIN col_name := TG_ARGV[0]; RETURN NEW; END $$;", "ok");
    [Fact]
    public void pptbb0026() => CorpusAssert.Parses(@"CREATE FUNCTION s.trg_bad_syntax_declare() RETURNS trigger LANGUAGE plpgsql AS $$ DECLARE x RETURN NEW; END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptbb0027() => CorpusAssert.Parses(@"CREATE FUNCTION bad_trg_syntax_if() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF TG_OP = 'INSERT' RETURN NEW; END IF; END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptbb0028() => CorpusAssert.Parses(@"CREATE FUNCTION bad_trg_syntax() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN RETURN NEW END $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptbb0029() => CorpusAssert.Parses(@"CREATE FUNCTION bad_trg_begin_only() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN $$;", "error");
    [Fact(Skip = "pending: parser not yet complete")]
    public void pptbb0030() => CorpusAssert.Parses(@"CREATE FUNCTION bad_trg_no_lang() RETURNS trigger AS $$ BEGIN RETURN NEW; END $$;", "error");
}
