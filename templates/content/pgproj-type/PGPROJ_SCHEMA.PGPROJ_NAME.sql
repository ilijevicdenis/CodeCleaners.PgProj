-- Type PGPROJ_SCHEMA.PGPROJ_NAME. Replace the placeholder enum with your own definition
-- (e.g. AS (a int, b text) for a composite type).
CREATE TYPE PGPROJ_SCHEMA.PGPROJ_NAME AS ENUM ('first', 'second');
