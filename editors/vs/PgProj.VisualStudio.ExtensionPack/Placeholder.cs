// This project is a content-only Visual Studio EXTENSION PACK: it ships no code of its own, only a
// manifest that bundles and installs the two PgProj extensions. This file exists solely so the
// net472 project has a compilation input; the resulting (empty) assembly is excluded from the VSIX
// via IncludeAssemblyInVSIXContainer=false.
namespace PgProj.VisualStudio.ExtensionPack
{
}
